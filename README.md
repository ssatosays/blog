# blog
This repo is for my blog.

## setup
1. git clone
2. use submodule
    - `git submodule update --init --recursive`  # needed when reclone

## hugo commands
1. build
    - `hugo --minify`
2. server
    - `hugo server`
